[package]
name = "qu-syntax"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Qu AST + recursive-descent parser (subset of docs/qu-grammar.ebnf)."

[dependencies]
qu-lexer = { path = "../qu-lexer" }
