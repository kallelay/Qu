# Vector counterpart of cpu_mul.qu -- SAME element count, SAME
# arithmetic, but Value::Vec, which map1/map2 never parallelize at any size.
v = randn(4000000, 1)
x = abs(v)
for r = 1 to 60
    x = v .* v
end for
print("{v[0]:.4f}")
