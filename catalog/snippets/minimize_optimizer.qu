# @name: Minimize a scalar objective
# @desc: L-BFGS minimization of a user function over a parameter vector.
result = minimize("objective_fn", x0, max_iter=500)
print("best params: {result.params}, value: {result.value:.6f}")
