# @name: Find peaks in a signal
# @desc: Locate local maxima above a height, with a minimum spacing.
pk = findpeaks(x, min_peak_height=0.5, min_peak_distance=10)
print("peaks: {pk.peaks}")
print("locations: {pk.locations}")
